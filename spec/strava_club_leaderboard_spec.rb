# frozen_string_literal: true

RSpec.describe StravaClubActivities do
  it 'has a version number' do
    expect(StravaClubActivities::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
